name: Main Staging Workflow

on:
  workflow_dispatch:
  pull_request:
    branches:
      - staging

jobs:
  build-angular:
    name: Build Angular
    uses: ./.github/workflows/ci-angular-app-staging.yml
    secrets: inherit
