name: core
description: "A flutter package used to configure dependencies and modules"
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.10.0
  flutter: ">=3.38.6"
resolution: workspace

dependencies:
  flutter:
    sdk: flutter

  dio: ^5.9.0
  dio_smart_retry: ^7.0.1
  flutter_secure_storage: ^10.0.0
  shared_preferences: ^2.5.4
  get_it: ^9.2.0
  injectable: ^2.7.1+4

  websocket: ^0.0.1
  app_config_core: ^0.0.1
  app_config_prefs: ^0.0.1
  app_config_secrets: ^0.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

  injectable_generator: ^2.8.1
  build_runner: ^2.10.4

flutter:
